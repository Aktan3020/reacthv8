import React from "react";
import classes from "../BookList/styles.module.scss"
const Spinner = () => {
  return( <><div className={classes.spin}><span>Spinner...</span></div>
  
  </>)
};

export default Spinner;
